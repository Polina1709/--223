/*#define _CRT_SECURE_NO_DEPRECATE
#include <stdio.h>

#include <stdlib.h>
#include <locale.h>

#define _USE_MATH_DEFINES

#include <math.h>

void main()

{
	setlocale(LC_ALL, "RUS");
	double m1, m2, r, F;
	double const G = 6.61 * pow(10, -11);
	printf("¬ведите массу первого тела:");
	scanf("%lf", &m1);
	getchar();
	printf("¬ведите массу второго тела:");
	scanf("%lf", &m2);
	getchar();
	printf("¬ведите рассто€ние между телами:");
	scanf("%lf", &r);
	getchar();
	F = G * m1 * m2 / (r * r);
	printf("—ила прит€жени€: %15.8lf", F);
}*/














